- Dashboard page with all the current info displayed and buttons to get mining info, peerinfo and other infos
- find and implement magic number generation
- free form input and output to komodod like the console in qt wallet
- delete residue from published files before closing the app
- settings page with all the restrictions on chain name, magic, komodod version, komodod hash, signature from publisher etc.,
- ability to generate key pair on the fly or import preexisting one
- cleanup datadirectory to start a smartchain with same name again
- store the "nLocalServices" in state and display in dashboard
- store all relevant data from the initial startup of the daemon and use it when needed
- in the index.vue file, refactor variables into objects. an object for each card should be ok
- refactor index.js to index.js, publisher.js, downloader.js etc